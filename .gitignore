# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# Environment — never commit real credentials. Use .env.example as the template.
#
# `.env.e2e` is generated per machine by `npm run db:e2e:prepare`: the database
# port is allocated by `prisma dev` and is not portable, so only its template is
# committed.
.env
.env.*
!.env.example
!.env.e2e.example

# Prisma generated client (regenerate with `npm run db:generate`)
packages/db/src/generated/

# Local Prisma dev database state (`prisma dev`)
.prisma/

# Dev email output — Nodemailer stream transport writes .eml files here
apps/api/.mail/

# Test / tooling output
coverage/
playwright-report/
test-results/
blob-report/
.playwright/

# Logs
*.log
npm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
# Local Claude Code settings — machine-specific permissions, not shared config
.claude/
.DS_Store
Thumbs.db
